# By default dev-client uses same-origin + devServer proxy (see craco.config.js).
# Set REACT_APP_USE_DIRECT_API=1 to connect directly to REACT_APP_API_ORIGIN.
REACT_APP_API_ORIGIN=https://localhost:3001
